package main

// import "fmt"

// basic for loop

// func main() {
// 	for i := 0; i < 5; i++ {
// 		fmt.Println(i)
// 	}
// }

// usage of range + array.ß

func main() {
	// arr := []int{1, 2, 3, 4, 5}
	// for i, c := range arr {
	// 	fmt.Println(i, c)
	// }

	// range over string

	// for i, c := range "this_string" {
	// 	fmt.Println(i, c)
	// }

	//range over map

	// m := map[string]string{"one": "1", "two": "2", "three": "3"}
	// for k, v := range m {
	// 	fmt.Println(k, v)
	// }

	// Runes:
	// These are data which do not get contained in a single byte and may take not than a byte for storage and they have to be properly accessed by accessing their starting point.

}
